env: development
app_name: delivery-intelligence
version: "0.1.0"
debug: false
gitlab:
  url: "https://gitlab.example.com"
  token: "not-set"
  api_version: "v4"
  timeout: 30
  max_retries: 3
  per_page: 100
logging:
  level: "INFO"
  format: "json"
  output: "stdout"
database:
  url: "sqlite:///delivery_intelligence.db"
  echo: false
